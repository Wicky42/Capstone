/** Ein Kategorie-Eintrag aus GET /api/public/categories */
export type CategoryOption = {
    value: string;
    label: string;
};

/** Typ-Alias für den Enum-Namen einer Produktkategorie (z. B. "HONIG") */
export type ProductCategoryValue = string;
